% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_db.R
\name{new_db}
\alias{new_db}
\title{Initialize a new project database}
\usage{
new_db(
  db_path = "./.sqlite",
  mapping_fn = NULL,
  mapping_id = "ID",
  mapping_taxon = "Taxon",
  genetic_code = NULL,
  dedup = FALSE,
  assemble_cpus = 6,
  assemble_memory = 24,
  assembler = "GetOrganelle",
  seeds_db =
    "https://raw.githubusercontent.com/smithsonian/MitoPilot/main/ref_dbs/getOrganelle/seeds/fish_mito_seeds.fasta",
  labels_db =
    "https://raw.githubusercontent.com/smithsonian/MitoPilot/main/ref_dbs/getOrganelle/labels/fish_mito_labels.fasta",
  getOrganelle = paste("-F 'anonym'", "-R 10 -k '21,45,65,85,105,115'",
    "--larger-auto-ws", "--expected-max-size 20000", "--target-genome-size 16500"),
  mitofinder_db =
    "https://raw.githubusercontent.com/Smithsonian/MitoPilot/refs/heads/main/ref_dbs/MitoFinder/fish_mito_sampler.gb",
  mitofinder = paste("--megahit"),
  maptoref_ref = NA_character_,
  maptoref = "--very-sensitive-local",
  maptoref_consensus = "-d 3 --min-BQ 20",
  maptoref_iter = 5L,
  maptoref_topology = NA_character_,
  max_paths = 10,
  max_scaffolds = 10,
  min_assembly_length = 500,
  annotate_cpus = 6,
  annotate_memory = 36,
  annotate_ref_db = "Chordata",
  annotate_ref_dir =
    "https://raw.githubusercontent.com/Smithsonian/MitoPilot/refs/heads/main/ref_dbs/Mitos2",
  mitos_opts = "--intron 0 --oril 0",
  trnaScan_opts = "-M vert -X 20",
  arwen_opts = "-mtx",
  aragorn_opts = "-m -gcstd",
  curate_cpus = 4,
  curate_memory = 8,
  curate_target = "fish_mito",
  curate_ref_db = "Metazoa_RefSeq235",
  max_blast_hits = 10,
  linear_complete = FALSE,
  curate_params = NULL,
  orf_cpus = 4,
  orf_memory = 8,
  orffinder_opts = "-s 1",
  orf_min_len = 300,
  orf_max_overlap = 0.1
)
}
\arguments{
\item{db_path}{Path to the new database file}

\item{mapping_fn}{Path to the mapping CSV file. Must contain columns "ID", "Taxon, "R1", and "R2"}

\item{mapping_id}{Column name of the mapping file to use as the primary key}

\item{mapping_taxon}{Column name of the mapping file containing a Taxonomic
identifier (eg, species name)}

\item{genetic_code}{Optional NCBI translation table override. Default `NULL`
auto-selects from the curation ruleset (`curate_target`); a number sets an
override on the default curate_opts set.
https://www.ncbi.nlm.nih.gov/Taxonomy/Utils/wprintgc.cgi}

\item{dedup}{Run fastp with `--dedup` to drop PCR and optical duplicates
before assembly? Default FALSE keeps `--dont_eval_duplication`, the shipped
fastp default. Editable later in the preprocess-options modal.}

\item{assemble_cpus}{Default # cpus for assembly}

\item{assemble_memory}{default memory (GB) for assembly}

\item{assembler}{Assembler, choice of "GetOrganelle" (default), "MitoFinder",
or "MapToRef"}

\item{seeds_db}{Path to the gotOrganelle seeds database, can be a URL, cannot have same file name as labels_db.
Default is a fish database built from RefSeq. https://raw.githubusercontent.com/smithsonian/MitoPilot/main/ref_dbs/getOrganelle/seeds/fish_mito_seeds.fasta}

\item{labels_db}{Path to the gotOrganelle labels database, can be a URL, cannot have same file name as seeds_db.
Default is a fish database built from RefSeq. https://raw.githubusercontent.com/smithsonian/MitoPilot/main/ref_dbs/getOrganelle/seeds/fish_mito_labels.fasta}

\item{getOrganelle}{Default getOrganelle command line options}

\item{mitofinder_db}{Path to MitoFinder reference db, must be GenBank format (.gb), can be a URL.
Default is a ten-species fish mitogenome sampler (https://raw.githubusercontent.com/Smithsonian/MitoPilot/refs/heads/main/ref_dbs/MitoFinder/fish_mito_sampler.gb)}

\item{mitofinder}{Default MitoFinder command line options}

\item{maptoref_ref}{Path or URL of the MapToRef reference mitogenome. A
single-record GenBank file (.gb) is preferred; a FASTA is accepted but then
\code{maptoref_topology} must be set. Required when assembler = "MapToRef".}

\item{maptoref}{Default bowtie2 options for MapToRef}

\item{maptoref_consensus}{Default samtools consensus options for MapToRef}

\item{maptoref_iter}{Maximum MapToRef iteration passes (default = 5)}

\item{maptoref_topology}{Topology of a FASTA MapToRef reference, "circular"
or "linear". Ignored for GenBank references, where the LOCUS line wins.}

\item{max_paths}{Maximum number of assembly paths allowed for a sample to
continue past the Assemble step (default = 10). Samples exceeding this are
flagged as failed and skipped by downstream steps in WF1.}

\item{max_scaffolds}{Maximum number of scaffolds (within a single path)
allowed for a sample to continue past the Assemble step (default = 10).
Samples exceeding this are flagged as failed and skipped by downstream
steps in WF1.}

\item{min_assembly_length}{Minimum contig length (bp) to retain for
coverage calculations, BLAST, and annotation. Scaffolds shorter than this threshold are stored
but flagged as ignored (default = 500).}

\item{annotate_cpus}{Default # cpus for annotation}

\item{annotate_memory}{Default memory (GB) for annotation}

\item{annotate_ref_db}{Default Mitos2 reference database}

\item{annotate_ref_dir}{Default Mitos2 reference database directory}

\item{mitos_opts}{Default MITOS2 command line options}

\item{trnaScan_opts}{Default tRNAscan-SE command line options}

\item{arwen_opts}{Default ARWEN command line options}

\item{aragorn_opts}{Default ARAGORN command line options}

\item{curate_cpus}{Default # cpus for curation}

\item{curate_memory}{Default memory (GB) for curation}

\item{curate_target}{Default target database for curation}

\item{curate_ref_db}{Default curation reference database (default =
"Metazoa_RefSeq235", the only bundled DB with rRNA BLAST references)}

\item{max_blast_hits}{Maximum number of top BLAST hits to retain (default = 10)}

\item{linear_complete}{Treat linear assemblies as complete genomes for the
export "completeness" field? By default only circular assemblies are
labeled "complete genome" and linear assemblies "partial genome". Set TRUE
for taxa whose complete mitogenome is genuinely linear (default = FALSE).
Editable later in the curation-options modal.}

\item{curate_params}{Default curation parameters}

\item{orf_cpus}{CPUs for the optional ORF-finder step (default = 4)}

\item{orf_memory}{Memory (GB) for the optional ORF-finder step (default = 8)}

\item{orffinder_opts}{Default NCBI ORFfinder options (default = "-s 1")}

\item{orf_min_len}{Minimal ORF length in nucleotides (default = 300)}

\item{orf_max_overlap}{Maximum overlap with existing annotations, as a fraction
of the ORF length, before an ORF is discarded (default = 0.1)}
}
\description{
Initialize a new project database
}
