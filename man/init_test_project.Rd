% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_test_project.R
\name{init_test_project}
\alias{init_test_project}
\title{Initialize a test project}
\usage{
init_test_project(
  path = here::here,
  n = 2,
  executor = NULL,
  container = "drleopold/mitopilot:dev",
  config = NULL,
  Rproj = TRUE,
  force = FALSE
)
}
\arguments{
\item{path}{path the the directory for the test project (default = currect
working directory). Will be created if it does not already exists.}

\item{n}{how many samples to include in the test project (max = 8)}

\item{executor}{The executor to use for running the nextflow pipeline. Must
be one of "local" or "awsbatch".}

\item{container}{The container to use for running the pipeline.}

\item{config}{(optional) provide a path to an existing custom nextflow config
file. If not provided a config file template will be created based on the
specified executor.}

\item{Rproj}{(logical) Initialize and open an RStudio project in the project
directory (default = TRUE). This has now effect if not running
interactively in RStudio.}

\item{force}{(logical) Force recreating of existing project database and
config files (default = FALSE).}
}
\description{
This function will set up a test project and fectch associated data from ENA.
The `n` parameter can be used to limit the number of species used in the test
project for faster set up.
}
