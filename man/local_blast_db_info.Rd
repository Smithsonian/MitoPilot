% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blast_ref_utils.R
\name{local_blast_db_info}
\alias{local_blast_db_info}
\title{Read the local BLAST database provenance published by the pipeline}
\usage{
local_blast_db_info(dir_out)
}
\arguments{
\item{dir_out}{Project output directory (\code{session$userData$dir_out}).}
}
\value{
A named list with the parsed VERSION fields plus \code{built_date}
  (ISO date only), or NULL when no local search has run in this project or the
  file carries no usable build date.
}
\description{
The database lives inside the container, so the app cannot read it directly:
on HPC the container only exists on the compute nodes. Instead, blast_genbank
copies the database's VERSION file next to each sample's hits, and this reads
the most recently written copy.
}
\keyword{internal}
