% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circularize_asmb.R
\name{circularize_asmb}
\alias{circularize_asmb}
\title{Attempt to circularize a linear mitogenome assembly}
\usage{
circularize_asmb(
  assembly_fn = NULL,
  paired_reads_1 = "NA",
  paired_reads_2 = "NA",
  min_overlap = 220,
  min_identity = 99,
  min_junction_reads = 5,
  min_overhang = 30,
  cpus = 4,
  out_fn = NULL,
  log_fn = NULL,
  id = "sample",
  evidence_dir = NULL
)
}
\arguments{
\item{assembly_fn}{Path to the input assembly (fasta, single contig)}

\item{paired_reads_1}{Path to forward reads (fastq), or "NA" for none}

\item{paired_reads_2}{Path to reverse reads (fastq), or "NA" for none}

\item{min_overlap}{Shortest accepted self-overlap (bp, default = 220)}

\item{min_identity}{Percent identity required for the overlap (default = 99)}

\item{min_junction_reads}{Reads that must span the junction (default = 5)}

\item{min_overhang}{Bases a read must extend past the junction on each side
(default = 30)}

\item{cpus}{Number of CPUs for read mapping (default = 4)}

\item{out_fn}{Path for the output fasta. Defaults to overwriting nothing and
returning the result only.}

\item{log_fn}{Optional path for a plain-text log}

\item{id}{Sample ID, recorded in the evidence CSVs (default = "sample")}

\item{evidence_dir}{Optional directory to write `circularize_overlap.csv`
and `circularize_depth.csv` into}
}
\value{
(invisibly) a list with `circular` (logical), `sequence`
  (DNAStringSet), `trimmed` (bp removed) and `note` (human-readable summary)
}
\description{
Many assemblers emit a circular molecule as a linear contig whose end
duplicates its start. This function detects that redundant overlap by BLASTing
the contig against itself, trims the duplicated copy, and (when raw reads are
supplied) requires reads spanning the resulting junction before calling the
assembly circular.
}
\details{
The overlap detection follows the approach used by MitoHiFi's
`circularizationCheck` (MIT licensed, Genome Research Ltd).
}
