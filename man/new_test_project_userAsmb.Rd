% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_test_project_userAsmb.R
\name{new_test_project_userAsmb}
\alias{new_test_project_userAsmb}
\title{Initialize a test project for user-supplied assemblies}
\usage{
new_test_project_userAsmb(
  path = ".",
  n = Inf,
  full_size = FALSE,
  executor = "local",
  container = paste0("macguigand/mitopilot:", utils::packageVersion("MitoPilot")),
  Rproj = TRUE,
  force = FALSE,
  ...
)
}
\arguments{
\item{path}{path to the directory for the test project (default = current
working directory). Will be created if it does not already exist.}

\item{n}{how many samples to include in the test project (Default = Inf,
include all)}

\item{full_size}{(logical) Use the full size raw reads (default = FALSE).
Setting to TRUE will download the reads from ENA, which will require
several GB and will take some time to complete. By default the smaller
pre-filtered read files packaged with MitoPilot are used.}

\item{executor}{The executor to use for running the nextflow pipeline. A
built-in template ("local" (default), "awsbatch", "slurm", "sge", "pbs",
"lsf", "NMNH_Hydra", "NOAA_SEDNA") or a saved profile from
[generate_config()]. See [list_configs()].}

\item{container}{The container to use for running the pipeline.}

\item{Rproj}{(logical) Initialize and open an RStudio project in the project
directory (default = TRUE). This has no effect if not running
interactively in RStudio.}

\item{force}{(logical) Force recreating of existing project database and
config files (default = FALSE).}

\item{...}{Additional arguments passed to [new_project_userAsmb()]}
}
\description{
Sets up a test project for the user-assembly workflow, with nine samples
that between them cover the shapes an assembly can arrive in: a linear
mitogenome, a circular one, one that is circular but reported as linear, and
six multi-contig assemblies holding one, two, none, or one split across
three contigs. The assemblies ship with the package; the raw reads are the
same fish data used by [new_test_project()].
}
\details{
The project is created with the mitogenome search and the circularization
attempt both switched on, since that is what these samples are for.
}
