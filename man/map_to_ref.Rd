% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_to_ref.R
\name{map_to_ref}
\alias{map_to_ref}
\title{Map-to-reference mitogenome assembly}
\usage{
map_to_ref(
  id,
  ref,
  reads_1,
  reads_2,
  bowtie2_opts = "--very-sensitive-local",
  consensus_opts = "-d 3 --min-BQ 20",
  iter_cap = 5,
  topology = NA_character_,
  genetic_code = NA_integer_,
  cpus = 4,
  out_dir = ".",
  ref_value = NA_character_
)
}
\arguments{
\item{id}{Sample ID.}

\item{ref}{Path to the reference (.gb or FASTA, one record).}

\item{reads_1, reads_2}{Preprocessed paired reads.}

\item{bowtie2_opts}{Flags passed verbatim to bowtie2.}

\item{consensus_opts}{Flags passed to samtools consensus after validation.}

\item{iter_cap}{Maximum number of iteration passes.}

\item{topology}{"circular" or "linear"; required for a FASTA reference,
ignored for GenBank.}

\item{genetic_code}{The sample's genetic code, used only for a warning.}

\item{cpus}{Threads.}

\item{out_dir}{Output directory.}

\item{ref_value}{The reference exactly as the user configured it: an absolute
file path, a URL, or an NCBI nucleotide accession. An accession is
downloaded from GenBank here; anything else means \code{ref} is already
the staged reference file.}
}
\value{
invisibly TRUE on success, FALSE after writing the failure sentinel.
}
\description{
Maps a sample's reads to a reference mitogenome, feeds the consensus back in
as the next mapping reference until it stops changing, then calls the
published sequence from a final pass over all reads. The reference base never
enters the published sequence.
}
