% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_mito.R
\name{find_mito}
\alias{find_mito}
\title{Find the mitochondrial contigs in a user-supplied assembly}
\usage{
find_mito(
  assembly_fn = NULL,
  hits_fn = NULL,
  id = "sample",
  mitofinder_db = NULL,
  genetic_code = 2,
  min_identity = 70,
  min_aligned_length = 300,
  min_aligned_fraction = 0.5,
  max_candidates = 20,
  min_genes = 3,
  cpus = 4,
  out_dir = "."
)
}
\arguments{
\item{assembly_fn}{Path to the sample's full assembly (fasta)}

\item{hits_fn}{Chunk hit files written by the screen}

\item{id}{Sample ID, used to name the output fasta}

\item{mitofinder_db}{Path to a MitoFinder GenBank database, built with
[custom_assembly_db()] (`db_type = "mitofinder"`)}

\item{genetic_code}{NCBI translation table passed to MitoFinder}

\item{min_identity, min_aligned_length, min_aligned_fraction, max_candidates}{Screen thresholds, see [select_mito_contigs()]}

\item{min_genes}{Mitochondrial genes a contig must carry to be confirmed}

\item{cpus}{Number of CPUs for MitoFinder}

\item{out_dir}{Directory for the outputs}
}
\value{
(invisibly) a list with `confirmed`, `note` and `evidence`
}
\description{
Driver for the WF1 `find_mito_pick` process: merges the BLAST screen output,
selects candidate contigs, extracts them from the assembly with
`samtools faidx`, confirms them with MitoFinder, and writes the files the
pipeline consumes.
}
