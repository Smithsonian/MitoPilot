% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geome_db.R
\name{fetch_geome}
\alias{fetch_geome}
\title{Fetch GEOME metadata for project samples}
\usage{
fetch_geome(path = ".", ids = NULL, bcids = NULL)
}
\arguments{
\item{path}{Path to the project directory (default = current working directory)}

\item{ids}{Sample IDs to fetch. Default: every sample with a BCID.}

\item{bcids}{Optional BCIDs to set for `ids` first (same length as `ids`).
A blank value removes that sample's BCID and its GEOME data.}
}
\value{
Invisibly, a data frame of `ID`, `status`, and `message`.
}
\description{
Looks up each sample's GEOME BCID, walks up its parent records (e.g.
Tissue, Sample, Event) and adds expedition and project details, then stores
everything in the project database for viewing in the app and use at export.
}
