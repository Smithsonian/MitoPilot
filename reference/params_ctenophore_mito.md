# Default curation and validation parameters for ctenophore Mitochondria

Default curation and validation parameters for ctenophore Mitochondria

## Usage

``` r
params_ctenophore_mito(alt = list())
```

## Arguments

- alt:

  (optional) named list of default values to modify
