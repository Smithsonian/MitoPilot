# Default curation and validation parameters for Starfish Mitochondria

Default curation and validation parameters for Starfish Mitochondria

## Usage

``` r
params_starfish_mito(alt = list())
```

## Arguments

- alt:

  (optional) named list of default values to modify
