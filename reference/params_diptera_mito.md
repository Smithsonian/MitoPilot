# Default curation and validation parameters for dipteran Mitochondria

Default curation and validation parameters for dipteran Mitochondria

## Usage

``` r
params_diptera_mito(alt = list())
```

## Arguments

- alt:

  (optional) named list of default values to modify
