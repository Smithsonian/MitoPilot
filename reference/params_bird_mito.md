# Default curation and validation parameters for bird Mitochondria

Default curation and validation parameters for bird Mitochondria

## Usage

``` r
params_bird_mito(alt = list())
```

## Arguments

- alt:

  (optional) named list of default values to modify
