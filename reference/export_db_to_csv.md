# Export project database to CSV

Exports the MitoPilot SQLite database as a CSV. File name will include
the date and time of the export.

## Usage

``` r
export_db_to_csv(path = ".")
```

## Arguments

- path:

  Path to the project directory (default = current working directory)
