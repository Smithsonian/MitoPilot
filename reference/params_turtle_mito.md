# Default curation and validation parameters for turtle Mitochondria

Default curation and validation parameters for turtle Mitochondria

## Usage

``` r
params_turtle_mito(alt = list())
```

## Arguments

- alt:

  (optional) named list of default values to modify
