# Default curation and validation parameters for octocorals Mitochondria

Default curation and validation parameters for octocorals Mitochondria

## Usage

``` r
params_octocoral_mito(alt = list())
```

## Arguments

- alt:

  (optional) named list of default values to modify
